import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO: add real tests when Firebase emulator is configured
    expect(1 + 1, equals(2));
  });
}
